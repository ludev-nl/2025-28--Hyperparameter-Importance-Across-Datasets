name: static_analysis-unit_tests

on: [push]

jobs:

  static_analysis:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      # - uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.12'
      #     cache: 'pip'

      - uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: libpcre3 libpcre3-dev swig3.0

      - run: /usr/bin/ln -s swig swig3.0

      - run: which swig

      - run: swig -version

      # - run: pip install -r requirements.txt

      # - run: python -m flake8

  # unit_tests:
  #   runs-on: ubuntu-24.04
  #   steps:
  #     - uses: actions/checkout@v4

  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.12'
  #         cache: 'pip'

  #     - uses: awalsh128/cache-apt-pkgs-action@latest
  #       with:
  #         packages: libpcre3 libpcre3-dev swig3.0

  #     - run: ln -s swig swig3.0

  #     - run: pip install -r requirements.txt

  #     - run: python -m unittest tests/openmltests.py tests/fanovatests.py
